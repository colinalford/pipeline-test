
pipeline {
    agent { docker { image pipeline {     agent { docker { im 'node:6.3' } }
    stages {
        stage('build') {
            steps {
                sh 'npm --version'
            }
        }
    }
}
